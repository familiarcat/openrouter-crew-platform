Use this space for reusable creative presets, prompt fragments, and future dashboard-fed controls.

The runtime currently reads `pipeline.task.json`, `pipeline.mapping.json`, and per-run analysis artifacts from the active output directory.
