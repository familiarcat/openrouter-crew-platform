This directory holds After Effects automation assets for the Clippy Doom pipeline.

The pipeline currently generates a per-run JSX script into the active output directory so it can stay in sync with the latest audio analysis and scene plan.
